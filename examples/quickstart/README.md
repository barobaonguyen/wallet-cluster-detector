# Quickstart Example

This example is offline-safe. The watchlist uses placeholder wallets, and the swaps are synthetic.

```bash
python examples/quickstart/main.py
```

Replace the CSV with your own watchlist or run `clusterdetect discover` to build one.
