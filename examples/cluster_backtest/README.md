# Cluster Backtest Notebook

The notebook uses a small synthetic fixture and explores public example thresholds. It does not contain a real watchlist or tuned values.

```bash
papermill examples/cluster_backtest/notebook.ipynb /tmp/wcd-backtest.ipynb
```
