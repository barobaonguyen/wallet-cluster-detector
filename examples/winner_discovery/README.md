# Winner Discovery

The dry path uses only GeckoTerminal and DexScreener free public endpoints:

```bash
python examples/winner_discovery/run_discovery.py --dry
```

Full mode extracts early buyers from pool history and needs a Helius key.
